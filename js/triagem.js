// Funcionalidades de Triagem Odontológica
document.addEventListener('DOMContentLoaded', function() {
    const formTriagem = document.getElementById('form-triagem');
    
    if (formTriagem) {
        // Inicializar formulário de triagem
        inicializarTriagem();
    }
});

function inicializarTriagem() {
    // Código para inicializar a triagem
}

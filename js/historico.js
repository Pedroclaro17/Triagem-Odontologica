// Funcionalidades de Histórico de Triagens
document.addEventListener('DOMContentLoaded', function() {
    const divHistorico = document.getElementById('historico');
    
    if (divHistorico) {
        // Exibir histórico de triagens
        exibirHistorico();
    }
});

function exibirHistorico() {
    // Código para exibir o histórico de triagens
}
